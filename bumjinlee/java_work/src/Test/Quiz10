문제 10번
Q. 아래 코드를 보고 물음에 답하시오.
A = new Test();
B = new Test();

여기서 만들어진  A객체와 B객체는 같은 것인가?
같다면 왜 같은지 틀리다면 왜 틀린지 이유를 함께 서술하시오.

A. 다르다
이유: 같은 클래스에서 생성되었지만 A를 참조하는 객체와 B를 참조하는 객체,
     서로 참조하는 객체가 서로 다르기 때문이다.