package jan0114;
/*
import static jan0114.skillUtility.masTer;
import static jan0114.skillUtility.firSt;


public class ruleMystrcmp implements Uniqueskill {
    private int lv;

    public ruleMystrcmp(int l) {
        lv = l;
    }


    public int gradeUp(ruleMystrcmp other) {
        if (this.lv < other.lv) {
            return -1;
        } else if (this.lv > other.lv) {
            return 1;
        } else {
            return 0;
        }
    }
        public int gradeUp (level other){
            if (this.lv < other.getex()) {
                return -1;
            } else if (this.lv > other.getex()) {
                return 1;
            } else {
                return 0;
            }
        }
            public int wayToMaster (Object road){
                ruleMystrcmp other = (ruleMystrcmp) road;
                return gradeUp(other);
            }

            public int nextStepTofirst (Object road){
                level other = (level) road;

                return gradeUp(other);

            }


            public int compareto (Object road,int playerLV){

                int res = 0;

                switch (playerLV) {
                    case masTer:
                        res = wayToMaster(road);
                        break;
                    case firSt:
                        res = nextStepTofirst(road);
                        break;
                    default:
                        break;
                }

                return res;
    }

    @Override
    public int comparato(Object other, int road) {
        return 0;
    }
}

 */


