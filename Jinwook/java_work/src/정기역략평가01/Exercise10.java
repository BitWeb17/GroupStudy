package 정기역략평가01;

public class Exercise10 {
    // 10. 아래으 코드를 보고 물음에 답하시오
    // A = new Test() 와 B = new Test()가 같은가 다른가? 이유도 설명하시오


    // Answer :
    // new는 객체를 생성할때 사용하는 연산자 이다.
    // new를 통해 Heap영역에 데이터를 저장할 수 있는 공간을 할당 받는다
    // 즉 동적할당을 하는 연산자 이다.
    // 참조값을 반환 하여 주고 이어서 생성자를 호출한다.
    // 만약 위의 식이 같은거라고 하면 굳이 쓸 필요가 없다
    // 그렇기 때문에 위의 두개의 식은 다르다고 할 수 있다.
    // 그리고 생성자를 호출한 후 어떤 참조값을 리턴하여 객체에 반환을 하느냐에 따라 달라진다고 할 수 있다.




}
