const NewMapTest = () => {
    let mapData = new Map()

    console.log(mapData)
    // 출력 : Set(0) { }
    //        아무런 데이터 값이 없기 때문에 안뜬다.

    return (
        <div className="NewMapTest">
            <p>NewMapTest</p>
        </div>
    )
}

export default NewMapTest