import React from 'react'

function LiteralTest() {

    const first = 3;
    const second = 7;

    const str2 = `${first} * ${second} = ??`;

    console.log(str2);
    return (
        <div>
            
        </div>
    )
}

export default LiteralTest;
