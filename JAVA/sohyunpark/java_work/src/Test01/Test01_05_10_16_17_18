5) 알고 있는 리눅스 명령어를 5개 이상 적고 각각에 대해 설명해보도록 하자
cd 경로 이동
cd .. 상위 경로 이동
cd ~ 최상위 경로(홈 디렉터리)로 이동

ls 디렉터리 목록 확인
ls -a : all의 의미 가짐 -> 숨김 파일 포함 모든 내용 리스트 형식으로
ls -l 현재 파일의 세부사항
ls -al : a와 l의 옵션 결합

clear 화면 정리

mkdir 디렉터리 생성

rm -r 디렉터리 삭제
rm -f 파일 삭제
rm -rf 파일과 디렉터리 모두 삭제

mv 파일 이동
mv file1 file2 : file1에서 file2로 이동, 변경

pwd 현재 작업 중인 디렉터리

vi 파일을 작성하거나 수정하기 위한 편집기
i, a 입력시 입력 모드 전환
esc 종료 모드 전환
shift + a : 맨 뒷줄에서 입력 모드 시작
shift + i : 맨 앞줄에서 입력 모드 시작
a : 현재 커서 기준 바로 뒷글자부터 시작
i : 현재 커서 위치에서 시작
yy : 1줄 복사
p: 붙여넣기
d숫자d : 숫자만큼 지우기
y숫자y : 숫자만큼 복사하기
:wq 편집 후 vi 종료하기 위한 명령어

git clone 주소 : 해당 주소 내용 복사해옴
git status : 현재 변경 상태, 파일 추적 상태 보여줌
git commit -am "메시지" : 작업한 내용을 "메시지"로 저장
git push origin main : 커밋 후 깃허브 웹사이트에 내용 저장



10) A = new Test();  B = new Test();
    여기서 만들어진 A 객체와 B 객체는 같은 것인가?
    같다면 왜 같은지, 틀리다면 왜 틀린지 이유와 함께 서술하시오.
    -> 다르다. 서로 영향을 주지 않고 독립적이다.
       서로 별개의 메모리 공간을 제어해 A 객체와 B 객체는 서로 다르다.



16) 인터페이스의 사용 목적에 대해 기술해보도록 한다.
유지보수가 용이해진다.
같은 이름의 메소드를 서로 다른 클래스에서 사용 가능하다. -> 개발 시간 단축, 편리
여러 사람끼리 작업시 인터페이스를 통해 메소드를 정할 수 있다. -> 유연성이 높아진다.



17) 라우터와 스위치의 동작 방식에 대해 기술해보자
- 로컬망 동작 방식
    192.168.0.0 <--- switch ---> 192.168.0.100
 xx:6b:rt:xx:xx:6e              xx:6b:rt:xx:xx:6e
스위치는 기본적으로 내부망(로컬망) 전용으로, 우리의 사설IP 주소를 라우터에게 보냄
-> 라우터는 IP 주소를 통해 자신의 라우팅 테이블을 확인하여 네트워크를 상호 연결, 네트워크 간 데이터 전송.
라우터 : 적합한 경로를 찾아줌
스위치는 각 개인의 유일무이한 MAC 주소를 기반으로 경로를 찾아줌
스위치로 연결된 디바이스들은 서로 간 통신 가능

공유기는 NAT 프로토콜을 사용해 사설망의 IP를 공인 IP로 변환하여 인터넷을 사용한다.

- 특수한 스위치(L4)
네이버, 구글 <--> switch(L4) <--> router <--> switch <--> 우리의 사설망
L4스위치는 포트 번호(서비스 종류를 나타내는 번호)까지 포함해 데이터 송수신
* 고정 포트 번호 외에 새로 포트 번호 생성해 사용 가능 (고정된 포트 번호 할당시 다른 서비스들과 충돌 유발)
* 현재 포트 번호를 보면 내가 어떤 서비스에 접근하는지 알 수 있다.



18) 자바 컴파일러와 자바 인터프리터(JVM)에 대해 기술해보자
- 자바 컴파일러
작성된 소스 코드를 JVM이 이해할 수 있는 자바 바이트 코드로 변환
.java 파일을 javac가 바이트 코드로 쓰여진 .class 파일로 변환(JVM을 위해 기계어로 변환)

- 자바 인터프리터(JVM)
자바 바이트 코드를 실행시키기 위한 가상의 기계 -> 자바 인터프리터 : 바이트 코드를 읽고 해석
javac에 의해 변환된 클래스 파일 내 바이트 코드를 기계에서 실행될 수 있도록 변환

.java  --------------->  .class  ---------------->  Machine language
     javac(java compiler)        java interpreter