package Seventeenth;

public class TestQuestion10 {

    // A = new Test();
    // B = new Test();
    // 여기서 만들어진 A 객체와 B 객체는 같은것인가?
    // 같다면 왜 같은지 틀리다면 왜 틀린지 이유를 함께 서술하시오.

    // 답 : A 객체와 B 객체는 다릅니다.
    // A 와 B 는 서로 별도의 메모리 공간을 제어하는 것이기 때문에
    // 같은 값이 들어간다고 하더라도 값만 같을 뿐
    // 서로에 영향을 줄 수 없는 객체들입니다.

}
