package Seventeenth;

public class TestQuestion5 {
    // 1. mv -f 옵션 : 지정 위치에 동일 파일이 있을 경우 덮어 쓸 때 묻지 않는다.
    // 2. mv -b 옵션 : --backup 옵션과 동일, 지정 위치에 동일 파일이 있을 경우 백업 후 이동한다.
    // 3. dpkg -l : 설치된 패키지 목록 확인한다.
    // 4. dpkg <파일명> : 해당 파일명 또는 경로가 포함된 패키지들을 검색
    // 5. ls -a 옵션 (all) : 숨겨진 파일이나 디렉토리도 보여준다.
    // 6. ls -B 옵션 : ~로 끝나는 백업파일을 제외하고 출력한다.
    // 7. mkdir -m 옵션 : 디렉토리의 접근 권한을 설정한다.
    // 8. mkdir -p 옵션 : 여러 덱스의 하위 디렉토리를 생성시에 사용한다.
}
