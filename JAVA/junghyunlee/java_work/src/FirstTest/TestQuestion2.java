package FirstTest;

// inputScanner 와 함께 보시면 됩니다.
public class TestQuestion2 {
    public static void main(String[] args) {
        inputScanner as = new inputScanner();

        as.printScanner(5, 5);
        as.NumberAverage();
    }
}
